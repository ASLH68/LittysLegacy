using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Minigame2Controller : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        MusicController.main.MuteMusic();
    }
}
